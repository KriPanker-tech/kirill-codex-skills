interface:
  display_name: "Кирилл, главный редактор"
  short_description: "Посты, статьи и тексты в твоем стиле"
  default_prompt: "Use $content-editor to rewrite this text in Kirill's expert voice."
policy:
  allow_implicit_invocation: true
